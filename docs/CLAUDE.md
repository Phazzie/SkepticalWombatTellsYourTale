# CLAUDE.md (docs)

Extends `/CLAUDE.md`. Follow root rules first.

Scope: project documentation files.

- Keep docs concise, dated, and scoped to their topic.
- Update `docs/STATUS.md` and `CHANGELOG.md` when observable behavior changes.
- Never include secrets, environment variable values, or credentials in docs.
- Architecture decision records (ADRs) go in `docs/` with a date prefix.
